# Lily Frontend Environment Variables
# Copy this file to .env.local and fill in the values before running locally.

# Base URL used for canonical links and metadata. No trailing slash.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Base URL for the Lily Protocol API. No trailing slash.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
