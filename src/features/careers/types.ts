export interface OpenRole {
  id: string;
  title: string;
  team: string;
  location: string;
  type: string;
  applyHref: string;
}

export interface CultureValue {
  id: string;
  title: string;
  description: string;
}
